name: Validate

on:
  # Feature → Dev: Run validation on PRs to dev
  # Dev → Main: Run validation on PRs to main
  pull_request:
    branches: [dev, main]
  # Run on pushes to dev and main branches
  push:
    branches: [dev, main]

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest

    strategy:
      matrix:
        workspace: [frontend, backend]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ${{ matrix.workspace }}/package-lock.json

      - name: Install dependencies
        working-directory: ./${{ matrix.workspace }}
        run: npm ci

      - name: Run ESLint
        working-directory: ./${{ matrix.workspace }}
        run: npm run lint
        # TODO: Add "lint": "eslint . --ext .js,.jsx,.ts,.tsx" to package.json
        # TODO: Install eslint and configure .eslintrc.json

      - name: Check formatting with Prettier
        working-directory: ./${{ matrix.workspace }}
        run: npm run format:check
        # TODO: Add "format:check": "prettier --check \"src/**/*.{js,jsx,ts,tsx,json,css,md}\"" to package.json
        # TODO: Install prettier and create .prettierrc.json

  security:
    name: Security Scan
    runs-on: ubuntu-latest

    strategy:
      matrix:
        workspace: [frontend, backend]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ${{ matrix.workspace }}/package-lock.json

      - name: Install dependencies
        working-directory: ./${{ matrix.workspace }}
        run: npm ci

      - name: Run npm audit
        working-directory: ./${{ matrix.workspace }}
        run: npm audit --audit-level=moderate
        continue-on-error: true
        # Allow moderate vulnerabilities during development, fail on high/critical
        # Note: Snyk security scanning removed (optional external tool)
        # npm audit provides baseline dependency vulnerability checking

  secrets-scan:
    name: Scan for Secrets
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for better secret detection

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  typecheck:
    name: TypeScript Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Run TypeScript compiler check
        working-directory: ./frontend
        run: npx tsc --noEmit
        # Checks types without emitting files
