{
  "name": "embark-quoting-backend",
  "version": "1.0.0",
  "description": "Backend API for Embark Quoting System",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:unit": "echo \"TODO: Add Jest unit tests\" && exit 0",
    "test:integration": "echo \"TODO: Add integration tests\" && exit 0",
    "lint": "echo \"TODO: Add ESLint configuration\" && exit 0",
    "format:check": "echo \"TODO: Add Prettier configuration\" && exit 0",
    "migrate:test": "echo \"TODO: Add Drizzle migrations\" && exit 0"
  },
  "keywords": ["quoting", "landscaping", "pwa"],
  "author": "Embark Landscaping & Earthworks",
  "license": "PRIVATE",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}
